# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor / tooling
.vscode/
.idea/
node_modules/

# Packaged builds
*.zip
*.crx
*.pem

# Chrome writes this into an unpacked extension's folder when it loads
# (indexed declarativeNetRequest rulesets). Generated, never committed.
_metadata/
